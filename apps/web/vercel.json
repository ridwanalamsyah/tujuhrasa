{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push --accept-data-loss --skip-generate && tsx prisma/seed-prod.ts && next build",
  "installCommand": "cd ../.. && npm install",
  "regions": ["sin1"]
}
